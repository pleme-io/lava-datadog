# lava-datadog

Autogenerated typed `(deflava-resource …)` shapes for every datadog
terraform-provider resource. Mirrors pangea-datadog.

## Regenerate

```bash
tofu providers schema -json > schema.json
lava-forge generate --schema schema.json --out resources
```
